# Nixpacks configuration for Graham PDF Parser

[phases.setup]
aptPkgs = ["default-jre-headless"]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
